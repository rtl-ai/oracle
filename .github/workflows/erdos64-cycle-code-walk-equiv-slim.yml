name: Erdos64 cycle-code walk bridge slim

on:
  push:
    branches: ["ci/erdos64-cycle-code-walk-equiv"]
    paths:
      - "lean-proof-lab/**"
      - ".github/workflows/erdos64-cycle-code-walk-equiv-slim.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: erdos64-cycle-code-walk-equiv-slim
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-slim
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Compile and audit the reverse bridge
        shell: bash
        run: |
          set -Eeuo pipefail
          cd lean-proof-lab
          mkdir -p artifacts-walk-equiv-slim
          LOG="$PWD/artifacts-walk-equiv-slim/commands.log"
          : > "$LOG"

          run_logged() {
            local command="$1"
            printf '$ %s\n' "$command" | tee -a "$LOG"
            set +e
            bash -o pipefail -c "$command" >> "$LOG" 2>&1
            local rc=$?
            set -e
            printf 'EXIT_CODE=%d\n' "$rc" | tee -a "$LOG"
            if (( rc != 0 )); then exit "$rc"; fi
          }

          run_logged "git rev-parse HEAD"
          run_logged "curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none"
          export PATH="$HOME/.elan/bin:$PATH"
          run_logged "lean --version"
          run_logged "lake --version"
          run_logged "lake update"
          run_logged "git -C .lake/packages/mathlib rev-parse HEAD"
          run_logged "lake exe cache get"
          run_logged "lake build Erdos64FiniteCertificate 2>&1 | tee artifacts-walk-equiv-slim/lake-build.out"
          run_logged "lake env lean Erdos64FiniteCertificate/CycleCodeWalkEquiv.lean 2>&1 | tee artifacts-walk-equiv-slim/direct.out"
          run_logged "if grep -RInE '(^|[^[:alnum:]_])(sorry|admit)([^[:alnum:]_]|$)|^[[:space:]]*axiom[[:space:]]' Erdos64FiniteCertificate.lean Erdos64FiniteCertificate; then exit 1; fi"
          run_logged "if grep -q 'sorryAx' artifacts-walk-equiv-slim/lake-build.out artifacts-walk-equiv-slim/direct.out; then exit 1; fi"

          {
            printf 'repository_commit=%s\n' "$GITHUB_SHA"
            printf 'runner_os=%s\n' "$RUNNER_OS"
            printf 'runner_arch=%s\n' "$RUNNER_ARCH"
            printf 'overall_exit_code=0\n'
          } > artifacts-walk-equiv-slim/RUN.txt
          sha256sum lean-toolchain lakefile.toml Erdos64FiniteCertificate.lean \
            Erdos64FiniteCertificate/FiniteCertificateBridge.lean \
            Erdos64FiniteCertificate/CycleCodeBounds.lean \
            Erdos64FiniteCertificate/CycleCodeWalkEquiv.lean \
            > artifacts-walk-equiv-slim/source.sha256

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: erdos64-cycle-code-walk-equiv-slim-evidence
          path: lean-proof-lab/artifacts-walk-equiv-slim
          if-no-files-found: error
          retention-days: 30
